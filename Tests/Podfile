inhibit_all_warnings!
use_frameworks!

abstract_target 'Common' do
	pod 'Quick', '~> 2.0'
	pod 'Nimble', '~> 8.0'

	target 'FueledUtilsTests-ReactiveSwift' do
		platform :ios, '8.0'

		pod 'FueledUtils/ReactiveSwiftUIKit', path: '../'
	end

	target 'FueledUtilsTests-SwiftUI' do
		platform :ios, '13.0'

		pod 'FueledUtils/ReactiveCombineBridge', path: '../'
	end
end
